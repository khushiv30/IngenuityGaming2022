"use strict";
function add(x, y) {
    return x + y;
}
console.log(add(5, 10));
function add2(x, y) {
    return x + y;
}
console.log(add2("5", "10"));
let sub = (x, y) => console.log(`Result : ${x - y}`);
sub(5, 12);
